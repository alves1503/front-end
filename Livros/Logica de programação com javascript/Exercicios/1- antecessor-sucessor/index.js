const numero = Number(prompt("Digite seu numero:"));

const antecessor = numero - 1;
const sucessor = numero + 1;

console.log(`Seu numero é ${numero}, tem antecessor ${antecessor} e sucessor ${sucessor}`);
